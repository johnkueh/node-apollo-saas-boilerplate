import {
  createCustomer,
  createCard,
  createSubscription,
  listAllInvoices,
  handleWebhook
} from './stripe';

test('creates customer and returns a customer id', () => {});
test('updates customer with token', () => {});
test('subscribes customer to a plan', () => {});
test('lists all invoices for a customer', () => {});
test('runs callback on customer subscription deleted webhook', () => {});
test('runs callback on customer subscription created webhook', () => {});
test('runs callback on customer subscription updated webhook', () => {});
